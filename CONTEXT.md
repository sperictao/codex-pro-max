# CONTEXT

## Codex 配置看守（Codex Config Guard）

启动器的功能域：对 `~/.codex/` 下的配置类文件做**基于 schema 的参数托管与锁定**。

### 术语

- **托管参数（Managed Parameter）** — schema 中声明的一条可管参数。每个托管参数只属于一个参数组，并以一个看守文件为写入目标。
- **参数组（Parameter Group）** — 围绕同一配置目的组织的托管参数集合，可以跨越多个看守文件；每个托管参数只属于一个参数组。参数组分为产品提供且不可删除的内置组，以及用户管理的自定义组。_避免：用 Group 指代目标文件。_
- **自定义参数（Custom Parameter）** — 用户在 UI 增删的托管参数，id 以 `custom.` 开头，落盘于磁盘 schema；删除只停看守，不回滚已写入的值。
- **看守文件（Guard File）** — 一个受 Guard 管理的物理配置文件，是格式校验、备份和写入的边界。参数组可以引用多个看守文件。_避免：分组、Group。_
- **看守文件格式（Guard File Format）** — 看守文件显式声明的解析契约，取 `toml`、`json`、`markdown` 或 `plain_text`；格式不由文件扩展名在运行时隐式决定。
- **看守运行状态（Guard Runtime State）** — Guard 的运行或暂停状态，只决定是否执行轮询，不代表任何托管参数已经写入或锁定。_避免：用启动/禁用描述参数状态。_
- **子代理角色（Subagent Role）** — 一个可被 Codex 选择的具名子代理定义，显式声明模型、推理强度和指令；多个角色可以同时存在。_避免：配置档、预设。_
- **角色 ID（Role ID）** — 子代理角色的稳定机器标识，同时也是 Codex 的 `agent_type` 和 `~/.codex/agents/<id>.toml` 的文件名主体。格式为 `[a-z][a-z0-9-]{0,62}`，按大小写不敏感方式全局唯一；创建后不可修改。_避免：显示名称、可变文件名。_
- **角色显示名称（Role Display Name）** — 仅用于界面展示的可变名称，可以使用中文，不参与文件定位或运行时路由。
- **默认角色（Default Role）** — id 为 `default` 的受保护子代理角色，可以修改内容但不能重命名或删除。
- **未托管角色（Discovered Role）** — 在 Codex 配置中被发现、但尚未由 Guard 接管的子代理角色；只有用户明确纳入看守后才成为托管对象。_避免：自动导入角色。_
- **角色目录（Role Directory）** — Guard 在 AGENTS.md 托管区块中生成的可用角色清单，记录角色 ID、用途和选择条件；主代理依据该目录选择 `agent_type`，并固定使用 `fork_turns = "none"`，不在单次派生时覆盖角色文件中的模型和推理强度。
- **角色适用性（Role Applicability）** — 角色声明的模型、协议版本和推理强度是否被当前 Codex 模型目录支持。模型目录不可用或组合不再受支持时，角色配置仍保留，但不得启用或锁定。
- **停止管理（Stop Managing）** — 让 Guard 放弃一个子代理角色的托管权，同时保留其实际配置文件。_避免：删除、Disable。_
- **删除角色（Delete Role）** — 备份后删除一个非默认子代理角色的实际配置文件；与停止管理是两个独立动作。
- **配置草稿（Configuration Draft）** — 用户正在编辑、尚未通过完整校验的临时内容；草稿不属于 Guard 的持久期望状态。
- **检测记录（Detection）** — 对文件实际路径的检测结果（命中路径或“未找到”+ 时间戳），随 GuardFile 落盘；首次进入设置页自动检测一次，之后直接读记录，手动“检测”按钮才重扫；检测到路径与配置不一致时弹窗确认后才改配置。
- **启用（Apply）** — 校验并把托管参数的期望值写入看守文件，成功后保持未锁定。_避免：启动、Start。_
- **锁定（Lock）** — 校验并启用托管参数后进入持续看守；锁定期间轮询发现实际值与期望值不一致时自动改回。
- **解锁（Unlock）** — 停止托管参数的自动恢复，但保留已写入值和启用状态。
- **禁用（Disable）** — 解锁并退出参数托管，但不回滚已写入值。
- **批量操作（Batch Operation）** — 对全部参数或一个参数组执行启用、锁定、解锁或禁用的单次原子操作；任一成员失败时，整个操作不产生部分成功，同一时间只允许一个 Guard 写操作。
- **配置有效性（Configuration Validity）** — 托管值同时满足目标文件格式、参数语义和当前 Codex 能力约束的状态；无效配置不得写入或锁定。
- **校验诊断（Validation Diagnostic）** — 一条可定位、可修复的结构化校验结果，包含错误码、严重度、作用域、文件、字段、行列、说明和修复建议；诊断不会自动改值或静默套用默认值。
- **配置所有权（Configuration Ownership）** — Guard 对规范化文件路径、参数路径或 Markdown 托管区块的唯一写入责任；同一目标不得同时由两个 Guard 对象或 Guard 与已声明的外部集成共同拥有。
- **能力快照（Capability Snapshot）** — 最近一次成功读取的 Codex 模型目录及时间，只用于离线展示和页面内存草稿，不把草稿保存为 Guard 的持久期望状态，也不足以授权启用或锁定。
- **事务日志（Transaction Journal）** — 批量写入前建立的本地恢复依据，记录批次、阶段、目标和快照引用，不保存配置原文；事务日志建立失败时不得开始写入。
- **未完成事务（Incomplete Transaction）** — 应用退出或崩溃后仍留在事务日志中的批次；Guard 必须先恢复或明确升级为 Critical Error，才能重新启动相关文件的轮询和写入。
- **操作审计（Operation Audit）** — 本地保存的 Guard 操作与子代理运行核验摘要，不包含角色指令、任务提示、文件原文、密钥或完整用户路径。
- **生命周期状态（Lifecycle Status）** — 参数、角色、参数组或全局作用域的管理状态，取 Disabled、Applied、Locked 或 Mixed；只描述 Guard 是否启用/锁定，不表达配置是否健康。
- **健康状态（Health Status）** — 参数、角色、参数组或全局作用域的校验与运行状态，取 Healthy、Drifted、Invalid、Unsupported 或 Error；聚合优先级为 Error > Invalid > Unsupported > Drifted > Healthy，与生命周期状态并列显示。
- **轮询（Poll）** — 周期性比对锁定参数的实际状态与配置状态。
- **schema** — 描述托管参数集合的 JSON 文件。落盘于 launcher 配置目录，启动时与内置 schema 合并（同 id 磁盘覆盖内置——用户定制内置参数；但 `label_en` / `description_en` / `default_en` 英文资源始终以内置为准，磁盘独有条目保留），UI 完全由合并结果驱动。
- **漂移（Drift）** — 锁定参数的实际状态与配置值不一致。轮询发现漂移即自动改回（写入前备份），配置页记录上次校验/恢复时间，不弹通知。
- **备份（Backup）** — 任何写入前把目标文件当前内容复制到 `~/.codex/dashi-backups/<文件名>.<时间戳>.bak`，每文件保留 20 份，无 UI 还原入口。
- **路径（Path）** — TOML 参数在文件中的点分位置，如 `features.image_generation`、`features.multi_agent_v2.enabled`、`agents`（toml_absent 的目标）。写入/比对/删除都按路径在解析后的 TOML 树上定位，中间表不存在时写入会逐级创建。
- **apply_mode** — 参数写入/校验的方式，四选一：
  - `toml_key` — 写入/更新 TOML 某键；比对值。
  - `toml_absent` — 确保某 TOML section 不存在；它再出现就再删（用于 multi_agent_v1 的 `[agents]` 块）。
  - `file_overwrite` — 整文件内容即值；比对全文哈希。
  - `markdown_block` — 用 `<!-- dashi:begin/end 名称 -->` 标记圈定的 Markdown 托管区块；比对区块内容。

### 语义边界

- 锁定即只读：改值须先解锁 → 改 → 启用 → 再锁。
- 不回滚：解锁、禁用或暂停 Guard 只停止对应层级的看守，已写入的值留在原地。
- 批量全有全无：执行前校验整个作用域；失败时恢复本批次已修改的文件。批次失败恢复是事务一致性，不是禁用后的产品语义回滚。
- 错误局部隔离：无效文件阻塞该文件及包含它的批量操作；无关参数组仍可操作。全局批量操作遇到任一无效成员即整体拒绝。
- 每个看守文件必须显式声明格式。TOML 和 JSON 校验整文件语法并拒绝重复键；Markdown 托管标记不得重复、交叉或残缺；纯文本必须是 UTF-8 且不含 NUL。旧配置仅在迁移时根据内置路径或扩展名推断一次，无法可靠推断时由用户选择。
- 格式校验先于参数语义和 Codex 能力校验。诊断同时显示在字段、卡片和页面汇总层，可定位到只读原文的行列；产品只给出明确修复入口，不自动修复或静默降级。
- 参数组与看守文件正交：参数组表达配置目的，看守文件表达物理写入边界；一个参数只能属于一个参数组。
- 多个子代理角色同时存在，不通过切换同一个 `default` 角色的配置档实现。
- 子代理角色通过角色 ID、显示名称、说明、模型、推理强度和指令等结构化字段管理；原始 TOML 只用于查看和诊断，不提供第二条编辑路径。模型与推理强度不得隐式继承。
- 角色 ID、`agent_type` 和角色文件名主体三者恒等；新建角色不得与磁盘已有角色或当前 Codex 已知角色重名。既有角色只能通过明确的“纳入看守”流程接管；非默认角色改名采用复制为新角色后删除旧角色。
- 创建和编辑角色时只允许从当前 Codex 模型目录选择支持 V2 多代理的模型，并根据模型动态筛选推理强度；不提供自由文本模型名。已托管角色后来变为不受支持时保留配置并标记 Unsupported，同时阻塞其所在参数组及全局作用域的启用、锁定。
- Codex 模型目录不可读取时，最近一次能力快照可以支持展示和页面内存草稿，但不得保存为 Guard 的持久期望状态；保存角色、启用和锁定必须先实时刷新成功，解锁和禁用始终可用。读取失败标记 Error，只有确认能力已移除才标记 Unsupported。
- Guard 生成角色目录作为主代理选角入口；派生子代理时由角色文件提供模型与推理强度，调用固定 `fork_turns = "none"`，不再通过单次调用覆盖这些字段。运行时审计按 `agent_type` 对照角色期望值。
- 已有角色只发现、不自动接管；无效角色只显示诊断，不自动覆盖。停止管理保留文件，删除角色才移除文件。
- `default` 角色始终存在且不可重命名或删除；其他角色通过“复制为新角色，再删除旧角色”完成改名。
- 停止管理角色是原子事务：自动解锁、禁用并移出角色目录和 Guard 状态，保留角色文件并将其放回未托管角色区。删除非默认角色也是原子事务：先备份，再移除角色文件、目录条目和 Guard 状态；失败时整体恢复。`default` 不允许停止管理。
- 最多同时托管 32 个子代理角色；未托管角色全部显示，接近上限时预警，达到上限后拒绝新增或纳入。
- 新建角色必须显式填写 ID、名称、用途、模型、推理强度和指令，不隐式继承 `default`。复制角色只复制用途、模型、推理强度和指令，必须提供新 ID 和名称；副本从 Disabled、未锁定开始，不复制健康状态、生命周期和审计记录，首次启用前不创建角色文件。
- 角色显示名称最多 80 字符、用途最多 500 字符、指令最多 64 KiB；文本必须是 UTF-8，去除首尾空白后必填字段不得为空，并拒绝 NUL。角色目录只生成经过 Markdown 转义的单行 ID、名称、用途和选择条件，不复制完整指令。
- 纳入已有角色时，语法有效的未知 TOML 字段和注释原样保留并显示 Warning；只有当前 Codex 正式诊断明确拒绝时才升级为 Invalid。未知字段只在只读原文中展示，不可从结构化编辑器修改。
- 自定义参数组可以创建、重命名、排序和删除；非空组不得删除，内置组不得删除。
- 内置参数组固定在自定义组之前且不可改名或排序；自定义组可排序；内置“未分类”组固定最后并在为空时隐藏。内置参数和托管角色的归组由产品固定，自定义参数创建时必须选组，之后可在无 Guard 写事务时移动。
- 批量启用跳过已锁定成员；批量锁定会先启用未启用成员；批量解锁只影响锁定成员；批量禁用会先解锁全部成员。无需变化的成员不算失败。
- 无效草稿不进入持久状态；保存角色、启用、锁定和写后复查分别执行对应层级的校验。
- 角色编辑器使用纵向可折叠卡片；默认角色固定首位，其他角色按用户顺序展示，未托管角色进入独立发现区。桌面宽度下批量动作显示图标和文字，窄屏仅显示带 tooltip、ARIA 标签和可见焦点的图标。
- 生命周期与健康状态是两条独立状态轴；批量执行期间另显示 Busy 和进度，不以生命周期状态代替错误或漂移诊断。
- 组级启用、锁定、解锁、禁用不二次确认；全局启用和锁定须先确认影响的参数数和文件数，全局解锁和禁用直接执行；删除角色须确认目标文件与备份，停止管理和保存不确认。
- 全局和组级按钮固定按“启用、锁定、解锁、禁用”排列，使用 `CircleCheck`、`LockKeyhole`、`UnlockKeyhole`、`CircleOff`；不使用播放图标。禁用动作置于末尾并与前三项拉开间隔，不可执行的按钮保留位置并说明原因。
- 批量操作期间暂停轮询并拒绝其他写请求，不排队执行过期意图。
- 批量操作按预检、备份、写入、写后复查、恢复、完成报告阶段执行并返回批次 ID。预检失败保证零写入；写入或复查失败恢复整个批次。恢复失败时标记 Critical Error、暂停受影响文件轮询、保留快照并明确列出未恢复文件，只能显式重试恢复或打开备份目录，禁止声称回滚成功。
- 首次写入前必须持久化事务日志、快照和原内容哈希；每次实际落盘前再次核对哈希，发现外部抢写即中止并恢复本批次已写文件。应用启动时先处理未完成事务：证据完整时自动恢复，证据缺失或恢复失败时进入 Critical Error，相关文件保持禁止轮询和写入。
- 同一物理路径先解析为规范化真实路径再判定所有权；同一文件内普通参数共享写入协调器，但规范化参数路径不得重复。托管角色独占对应 `agents/<id>.toml`，AGENTS.md 角色目录只拥有自己的标记区块，fastctx 拥有的键不得纳入 Guard。
- 旧版整文件托管迁移时，期望内容与实际文件均有效且相同才自动解析为结构化 `default` 角色并保留生命周期；冲突或无效时必须由用户选择采用实际文件、采用已存期望值或安全默认值，选择前不写磁盘。迁移完成后移除旧整文件所有者，禁止同一文件被双重锁定。
- 迁移只自动处理旧版 `default` 整文件配置；其他 `agents/*.toml` 仍只进入发现区。内置参数显式映射到内置参数组，无组的旧自定义参数进入不可删除的内置“未分类”组。
- Guard schema 升级前备份旧 Launcher 配置并提升版本。迁移失败或等待用户处理冲突时，只暂停 Guard 写入和轮询，其他启动器功能继续可用；禁止静默重置。旧版本不承诺直接读取新 schema，降级前必须恢复升级备份。
- 操作审计只记录时间、作用域 ID、相对文件名、阶段、结果、错误码、计数，以及运行时观察到的 `agent_type`、模型和推理强度，按 30 天或 500 条先到者清理。事务日志是配置写入前置条件；普通诊断审计失败必须暴露错误，但不反向破坏已完成的配置事务。
- 验收必须覆盖状态机、四种格式、所有权冲突、事务各阶段故障注入、崩溃恢复、并发拒绝、迁移分支、角色数量边界、模型离线与 Unsupported、双状态聚合、键盘与窄屏交互，并通过类型检查、前端构建、Rust 全量测试和隔离 Codex 目录的真实桌面冒烟；自动化测试不得读写用户真实 `~/.codex`。
- 本期范围止于配置建模、分层校验、原子批量生命周期、逻辑分组、多角色管理和运行时核验；不增加原始 TOML 编辑器、云同步、远程配置下发、第三方 provider/Codex++ 管理、Codex 自动安装升级、子代理自动执行或跑分，也不改造现有服务启动逻辑。
- 看守生命周期：仅在 launcher 运行期间轮询（固定 60s），launcher 关闭即停。
- 看守运行开关本身只启动或暂停轮询，不写入目标值；全局“启用”批量动作会在完整预检后写入，锁定后的漂移恢复也会写入。
- 文件移出看守列表即不再生效：该文件的参数从视图消失，轮询同样跳过（列表是看守范围的唯一事实来源）。
- TOML 解析失败：跳过该文件全部校验并在分组显示错误，绝不重写整文件；文件缺失时启用则新建。

## Taskboard 集成

启动器的另一功能域：把 dashi-taskboard 应用打包进安装包并注入 Codex 桌面端。

### 术语

- **主仓库（Upstream）** — `chuspeeism/dashi-taskboard`，taskboard 代码的权威来源，变更以 PR 汇入。
- **Fork** — `sperictao/dashi-taskboard`，主仓库的 fork，向主仓库提 PR 的中转地。
- **Launcher 仓库** — `sperictao/dashi-taskboard-launcher`（本仓库），Tauri 壳。
- **Vendor 快照（Vendor Snapshot）** — taskboard 在 launcher 内 `vendor/dashi-taskboard/` 的 git submodule，指向 Fork、pin 具体 commit；升级由 launcher 显式 bump 指针。取代 v0.2.5 前的纯文件拷贝。
- **Bundle** — Tauri 构建时经 `tauri.conf.json` resources 映射打进安装包的 taskboard 文件集。

## 界面多语言（i18n）

启动器的横切关注：壳 UI 与 Rust 侧所有用户可见字符串的多语言。

### 术语

- **界面语言（Display Language）** — 启动器自身用户可见文本的语言。设置项三选一：跟随系统 / English / 中文。
- **跟随系统（Follow System）** — 默认取值。启动时按 OS 语言一次性解析：系统为中文则中文，其余一律英文。
- **默认语言（Default Language）** — 英文。跟随系统解析不到中文时的兜底，也是缺失翻译的兜底。
- **解析语言（Resolved Locale）** — 「跟随系统」经解析后得到的具体语言（en 或 zh-CN），启动时确定，改设置后立即重解析。

### 语义边界

- 覆盖启动器壳与 Rust 侧字符串（托盘、进程状态、错误消息）；vendor/dashi-taskboard 是独立应用，其 UI 语言不在此域。
- 切换即时生效：界面重渲染、托盘重建；已产生/已显示的消息不回溯重翻。
- 看守域的**托管参数** label/description 同样双语：schema 条目的 `label_en` / `description_en` 是英文资源，空则落回原文（自定义参数永远只有用户原文）。部分参数的 **default 值**（写入 codex 的内容，如 AGENTS.md 段落、default.toml 指令）也带 `default_en`：英文界面下期望值取 `default_en`，即**写入内容随界面语言**。语义后果：用户改过的 `state.value` 永远优先；未锁参数切语言会显示漂移（由用户决定重应用）；已锁参数切语言后 60 秒轮询内被自动恢复为新语言内容。无 `default_en` 的参数 default 值不随语言变化。
- 界面语言与文档语言无关：README、release notes 的双语是另一回事，不受此设置影响。

## FastCtx 集成

启动器的第三功能域：设置页「集成」区的开关，把 fastctx（`yc-duan/fastctx`，MCP 工具运行时）接入/摘除 Codex。

### 术语

- **接入（Integrate）** — 调 fastctx CLI `fastctx apply --yes` 完成注册：fastctx 自己写 `~/.codex/config.toml`（`[mcp_servers.fastctx]`、`features.code_mode.direct_only_tool_namespaces`、共享键 `tool_output_token_limit`）并固化二进制到 `~/.fastctx/bin/`；默认 Standard 输出档。与看守域的"启用"区分：接入对象是外部工具整体，不是单条托管参数。
- **摘除（Unapply）** — 调 `fastctx unapply --yes`：杀受管进程、移除 fastctx 配置、删除 `~/.fastctx` 受管数据；npm 全局包保留，可重新接入。
- **安装检测（Install Detection）** — 检测 fastctx 可执行文件是否在 PATH；每次进入「集成」区实时执行，不落盘、无记录。与看守域的"检测记录"同名不同义：看守检测持久化到配置且只比对文件路径，安装检测即时丢弃且测的是可执行性。
- **接入状态（Integration State）** — 以 `~/.codex/config.toml` 是否含 `[mcp_servers.fastctx]` 为唯一事实来源，启动器不存开关布尔值。
- **自检（Self-check）** — 接入成功后跑一次 `fastctx status`；FAIL 只 toast 警告，不阻塞、不回滚。

### 语义边界

- 启动器不写任何 fastctx 拥有的 TOML 键（`mcp_servers.fastctx`、`tool_output_token_limit`、`features.code_mode.*`），一律委托 CLI；这些键也不应加入看守，用户自行锁入造成的互相改回不管。
- 接受 apply 对 ChatGPT 桌面端配置的连带写入（fastctx 官方行为，两个 host 同时配置）。
- 接入/摘除后需重启 Codex 会话才生效，启动器只提示，不自动重启。
- fastctx 的安装、升级、输出档位、jobs 管理归 fastctx 自己的 TUI/CLI；启动器只做未安装引导（`npm i -g fastctx`）与「打开控制台」入口。

## 启动器壳（Shell）

启动器的横切关注：Tauri 桌面壳的进程模型、生命周期与系统交互边界。

### 术语

- **单实例（Single Instance）** — 应用同时只允许一个实例运行；第二次启动不新建进程，转为激活已有实例并显示主窗口（若最小化在托盘则恢复）。
- **自启动（Autostart）** — 开机登录时静默启动到托盘，不显示主窗口。设置项默认关，与 `minimize_to_tray_on_close` 相互独立，不复用。
- **窗口状态记忆（Window State）** — 记忆主窗口尺寸/位置/最大化，启动时恢复；恢复位置落在所有显示器可视区之外时放弃恢复、改为居中。
- **壳通知（Shell Notification）** — 系统级通知，触发点仅限受管子进程的生命周期事故（意外退出、启动失败）。
- **壳日志（Shell Log）** — Rust 侧日志落盘于 app log dir，release 级别 Info，单文件上限 2MB，超限后轮转（旧文件直接删除，仅保留当前一份）；设置页提供「打开日志目录」入口。

### 语义边界

- 多开即事故：看守轮询会对同一批 codex 文件双写、taskboard 子进程重复 spawn 抢端口，故以单实例强制禁止，不靠用户自觉。
- 壳通知不覆盖看守域：漂移恢复保持静默（沿用看守域「不弹通知」边界）；updater 流程不弹（用户手动发起，UI 已覆盖进度与结果）。
- 日志只进文件不进 UI：消费方式是「出事再翻」，无应用内日志查看器，不告警、不上报。

## 界面主题（Theming）

启动器的横切关注：壳 UI 的色彩主题系统（tweakcn token 化重构后，daisyUI 已移除）。

### 术语

- **主题族（Theme Family）** — 一个 tweakcn 预设的本地化实例，亮暗原生成对。全量 41 族，由 `src/theme-families.ts`（构建脚本生成的 manifest）枚举，选择器中的可选项即 manifest 内容。
- **模式（Theme Mode）** — 三选一：跟随系统 / 亮 / 暗。与主题族正交。
- **解析主题（Resolved Theme）** — 族 × 模式解析出的主题名，命名约定 `<族id>-light|dark`，赋给 `<html data-theme>`，对应 `src/themes.css` 中同名 scoped token 块；跟随系统时随 OS 切换重解析。
- **默认族（Default Family）** — `vercel`，视觉基准。localStorage 中的族值不在 manifest 时静默回落默认族。
- **色板卡（Swatch Card）** — 主题族选择器项：卡面自身带 `data-theme` 局部生效，直接渲染该族亮主题的色板缩略。
- **主题构建脚本（Theme Build）** — `scripts/build-themes.mjs`：按写死的 41 个预设 id 从 tweakcn registry 拉取 token 与字体引用，生成 `src/themes.css`（scoped 块 + `@font-face`）与 `src/theme-families.ts`；生成物提交进 git、不手改，重跑脚本即主动跟随上游（上游新增预设不自动进入，id 列表是唯一事实来源）。
- **字体本地化（Local Fonts）** — 预设引用的 Google 字体 woff2 全部由构建脚本下载进 `assets/fonts/`，CSP 保持 `font-src 'self'`，主题字体完全离线可用。

### 语义边界

- 主题选择器只列亮族：用户不直接选暗色主题，暗面永远由命名约定的配对决定（「每主题适配亮暗」的唯一语义）。
- 主题持久化在 localStorage（`theme` 模式、`theme-family` 族），不进 LauncherConfig、不同步 Rust；语言在 LauncherConfig，两者互不影响。
- 视觉基准以默认族 vercel 为准：其余 40 族是增值选项，组件样式不得为任何族写特例。
- 字体随族：各族的 font token 是该族外观的组成部分，产品不承诺单一品牌字体。

// @ts-nocheck
// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
async guardGetView() : Promise<Result<GuardView, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_get_view") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardSetEnabled(enabled: boolean) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_set_enabled", { enabled }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardSetValue(id: string, value: JsonValue) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_set_value", { id, value }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardApply(id: string) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_apply", { id }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardSetApplied(id: string, applied: boolean) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_set_applied", { id, applied }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardSetLocked(id: string, locked: boolean) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_set_locked", { id, locked }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardAddCustomParam(param: GuardParam, fileId: string) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_add_custom_param", { param, fileId }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardRemoveCustomParam(id: string) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_remove_custom_param", { id }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardGetSchemaFilePath() : Promise<Result<string, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_get_schema_file_path") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardGetFiles() : Promise<Result<GuardFile[], string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_get_files") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardAddFile(name: string, file: string, format: GuardFileFormat) : Promise<Result<GuardFile, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_add_file", { name, file, format }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardUpdateFile(id: string, name: string, file: string) : Promise<Result<GuardFile, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_update_file", { id, name, file }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
async guardRemoveFile(id: string) : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_remove_file", { id }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * 检测文件实际路径并落盘记录；之后直接读记录，不重复扫盘
 */
async guardDetectFile(id: string) : Promise<Result<GuardFile, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_detect_file", { id }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * 把文件选择器选中的绝对路径换算为相对 ~/.codex 的路径（越界拒绝）
 */
async guardRelativizePickedPath(absPath: string) : Promise<Result<string, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_relativize_picked_path", { absPath }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * 返回启动恢复是否阻断了 Guard 写入。只暴露稳定 code，不泄漏 journal 细节。
 */
async guardGetRecoveryStatus() : Promise<Result<GuardRecoveryStatus, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_get_recovery_status") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * 重试未完成事务恢复；成功后按需启动唯一的 Guard 轮询任务。
 */
async guardRetryRecovery() : Promise<Result<null, string>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("guard_retry_recovery") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/

export const GUARD_CONTRACT = {"applyModes":["toml_key","toml_absent","file_overwrite","markdown_block"],"fileFormats":["toml","json","markdown","plain_text"],"paramStatuses":["match","drift","missing","error"],"schemaVersion":1,"valueTypes":["bool","int","string","text","none"]} as const;

/** user-defined types **/

export type CodexGuardState = { enabled?: boolean; params?: Partial<{ [key in string]: GuardParamState }>;
/**
 * 看守目标文件列表（内置 + 自定义）
 */
files?: GuardFile[] }
/**
 * 一次路径检测的结果记录
 */
export type DetectRecord = {
/**
 * 检测到的相对 ~/.codex 路径；None = 未找到该文件
 */
path: string | null; at: number }
export type GroupView = { id: string; name: string; file: string; format: GuardFileFormat; builtin: boolean; error: string | null; params: ParamView[] }
/**
 * 看守目标文件
 */
export type GuardFile = { id: string; name: string;
/**
 * 相对 ~/.codex 的路径
 */
file: string;
/**
 * toml | json | markdown | plain_text
 */
format: GuardFileFormat; builtin?: boolean;
/**
 * 上次路径检测记录；None = 从未检测（检测走文件系统，落盘后不再重复扫）
 */
detection?: DetectRecord | null }
/**
 * 看守文件的物理解析格式。序列化值是稳定的 snake_case 字符串。
 */
export type GuardFileFormat = "toml" | "json" | "markdown" | "plain_text"
/**
 * schema 中的一条托管参数
 */
export type GuardParam = { id: string; label: string;
/**
 * 英文 label；空 = 无英文资源，落回 label 原文（自定义参数即如此）
 */
label_en?: string; description?: string;
/**
 * 英文 description；空 = 落回 description 原文
 */
description_en?: string;
/**
 * 相对 ~/.codex 的路径，如 config.toml / AGENTS.md / agents/default.toml
 */
file: string;
/**
 * toml_key | toml_absent | file_overwrite | markdown_block
 */
apply_mode: string;
/**
 * 点分 TOML 路径（toml 模式用）
 */
path?: string;
/**
 * bool | int | string | text | none
 */
value_type?: string; default?: JsonValue;
/**
 * 英文 default（写入内容本身双语时用）；Null = 内容不随界面语言变化
 */
default_en?: JsonValue;
/**
 * 是否为用户自定义参数（非内置）；自定义参数可删除
 */
custom?: boolean }
/**
 * 单个参数的托管状态，持久化在 LauncherConfig.codex_guard.params
 */
export type GuardParamState = {
/**
 * 用户改后的值；None = 用 schema 推荐值
 */
value?: JsonValue | null; applied?: boolean; locked?: boolean; last_checked?: number | null; last_restored?: number | null }
/**
 * 前端可见的恢复状态。只返回稳定 code，不暴露 journal 路径、文件内容或底层错误。
 */
export type GuardRecoveryStatus = { blocked: boolean; code: string | null }
export type GuardView = { schemaVersion: number; enabled: boolean; groups: GroupView[] }
export type JsonValue = null | boolean | number | string | JsonValue[] | Partial<{ [key in string]: JsonValue }>
export type ParamView = { id: string; label: string; description: string; applyMode: string; valueType: string; path: string; default: JsonValue; value: JsonValue; applied: boolean; locked: boolean; actual: string | null;
/**
 * match | drift | missing | error
 */
status: string; error: string | null; lastChecked: number | null; lastRestored: number | null; custom: boolean }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}

# v0.2.6

修复一键启动失败 `启动后立即退出 (exit status: 1): address: '127.0.0.1', port: 47823`（EADDRINUSE）的问题。

## Bug Fixes

- 修复一键启动时 taskboard 服务 EADDRINUSE 秒退：上次启动残留的服务进程仍占用端口且运行健康时，启动器再 spawn 一个只会端口冲突。现在启动前先探测 `http://<host>:<port>/health`，健康则直接复用并在界面标注"复用已在运行的 Taskboard 服务"；复用的实例不属于启动器，停止按钮不会误杀外部进程

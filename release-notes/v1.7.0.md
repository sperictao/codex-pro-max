# v1.7.0

内置 taskboard 大幅升级：从 v1.0.3 时代快照同步至 v1.0.9（合并上游 152 个提交），保留 launcher 运行时端点发现、token 注入与注入器修复。

## Features

- **内置 dashi-taskboard 升级至 v1.0.9**：合并上游最新功能，包括任务对话链（Task conversation chain）、议题创建与关系选择（Issue creation & relations）、AI 对话（AI Chat）、Windows 原生支持、Composer 官方补全、重复事件详情（Recurrence detail）、Markdown 渲染增强（Mermaid / kaTeX / TOML 支持）等
- **launcher 集成保持**：`taskctl` 的 `CODEX_TASKBOARD_URL` 注入与 `~/.codex-pro-max/launcher-runtime.json` 运行时端点发现、注入器的 Windows 页面识别与 avatar composition surface 排除、语义导航回退全部保留

## Changes

- vendor 子模块指针更新：`79bbd5a` → `bcac4f9`（`sperictao/dashi-taskboard` main）
- 上游 taskboard 新增依赖（dompurify、js-yaml、mermaid、smol-toml、vitest 等）随打包资源一并合入

## 验证

- taskboard `typecheck`、`build:web` 通过；注入器/任务 CLI 关键测试 76 个全过（inject / injector / codex-targets / cli）
- launcher `pnpm run check:release -- --tag v1.7.0` 通过
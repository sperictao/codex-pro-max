## Changes

- Repackage the bundled `dashi-taskboard` at `c9fb80c` (`v1.1.23`) after reviewing the current upstream runtime changes. New runtime files remain covered by the existing `server` / `shared` / `scripts` / `cli` / `inject` resource roots, so the bundle whitelist is not widened unnecessarily.
- Switch vendored Taskboard dependency installation from `npm install` to `npm ci` in release builds and CI, making the packaged web UI and runtime dependencies follow the submodule lockfile exactly.
- Strengthen release preflight: initialize the pinned Taskboard submodule when needed, verify the required Taskboard resource contract, and validate the `dashi-taskboard` Node engine requirement before packaging.
- Sync the release version to `1.22.3` across `package.json`, Tauri config, `Cargo.toml`, and the root package entry in `Cargo.lock`; release validation now checks all four locations.

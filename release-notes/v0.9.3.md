# v0.9.3

## Fixes

- 修复 macOS 主窗口在隐藏创建后首次显示时可能无法响应鼠标和键盘输入的问题；主窗口改为可交互创建，自启动场景在初始化后隐藏到系统托盘
- 完成壳 UI 的 Tailwind CSS v4 + daisyUI v5 主题重构：支持 Geist 与内置亮色主题族的亮/暗/跟随系统模式，未配对暗色回落 `dark`，并消除首屏主题闪烁
- Geist Sans/Mono 改为应用内自托管字体，边框 token 使用 translucent `gray-alpha-400`；主题选择器补齐 `aria-pressed` 选中语义

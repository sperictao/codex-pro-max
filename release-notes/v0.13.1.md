# v0.13.1

## Features

- **集成页独立为顶栏页面**：集成从设置侧栏移除，顶栏新增「集成」入口（位于「看守」右侧）直达独立页面，一处即达 FastCtx 的接入开关、状态与控制台
- **FastCtx 更新胶囊**：集成页状态行右侧新增胶囊，检测到 npm 有新版本时显示最新版本号（如 `v1.2.3`）；离线或已是最新时自动隐藏

## Fixes

- **FastCtx 版本检测不再卡界面**：`fastctx_detect` 改为异步执行，查询 npm 最新版本在后台进行，不再占用 UI 主线程
